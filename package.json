{
  "name": "x-ranking",
  "version": "1.0.0",
  "private": true,
  "description": "X-only realtime trend, keyword, account and post monitoring on Cloudflare Workers + D1",
  "scripts": {
    "postinstall": "node scripts/prepare-cloudflare-config.mjs",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy --no-x-provision"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.45.0"
  }
}
