name = "x-ranking"
main = "src/index.ts"
compatibility_date = "2026-01-01"
keep_vars = true

# Cloudflare Dashboard remains the source of truth for Secrets and the existing D1 database.
# Workers Builds runs scripts/prepare-cloudflare-config.mjs, which resolves that existing D1
# and writes the ephemeral deploy config with a one-minute base Cron.
# The Worker only calls X when the app-level interval stored in D1 is due.
