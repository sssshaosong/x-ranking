name = "x-ranking"
main = "src/index.ts"
compatibility_date = "2026-01-01"

# 每 20 分钟执行一次。
[triggers]
crons = ["*/20 * * * *"]

# D1 使用 Wrangler 4 的自动 provisioning。
# 只声明 binding，不把账号相关 UUID 写进仓库。
# 首次部署会自动创建/关联 D1，之后保持同一绑定。
[[d1_databases]]
binding = "DB"
